/*typedef struct Invite_t {
	DWORD dwPlayerId;
	bool bInvite;
	bool bLoot;
} INVITE, *LPINVITE;*/

class Invite
{
public:
	Invite();
	~Invite();
	//static DWORD WINAPI InviteThread(LPVOID Parameter);
};

